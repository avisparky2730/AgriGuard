services:
  - type: web
    name: agriguard-ai-tn
    runtime: static
    buildCommand: npm run build
    staticPublishPath: app/dist
    envVars:
      - key: NODE_VERSION
        value: "20"
